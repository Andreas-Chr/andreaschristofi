# Asset provenance

Owner-supplied production root: `/Users/andreaschristofi/Desktop/andreaschristofi`.

| Destination | Source and treatment |
| --- | --- |
| `public/assets/fonts/` | `_exports/WOFF2`, original bytes and supplied OFL license notices; self-hosted with `font-display: swap`. |
| `public/assets/logo/`, `icons/`, `tools/`, `hero/` | `_exports/SVGs`, original vectors with viewBox/masks/embedded texture preserved; lowercase filenames. |
| `public/assets/logo/header-logo.svg` | Exact export returned by Figma for Home's header logo composition. |
| `public/assets/awards/` | `#assets/Awards/_icons`, original award marks. |
| `public/assets/process/` | Figma Process artwork nodes: desktop `5303:4229`, `5303:4246`, `5303:4264`, `5303:4285`, `5303:4307`; mobile `5081:1735`, `5082:3146`, `5082:3159`, `5082:3173`, `5082:3188`. SVG labels remain part of the exported decorative diagrams; accessible phase titles/descriptions are HTML. |
| `src/assets/andreas-bw.png` | `_exports/PNGs/andreas_bw.png`; original portrait, responsive derivatives generated by Astro. |
| `src/assets/cssda-*.png` | Four original `_exports/PNGs` certificates; 540px derivatives generated by Astro. |
| `src/assets/gradient-waves.png` | Exact bitmap asset returned by Figma. Astro creates an optimized WebP fallback. The raw image receives the same vertical transform as the Figma instance; the live renderer is independently flipped once. |
| `public/assets/documents/andreas-christofi-cv.pdf` | `_exports/PDFs/CV.pdf`, unchanged. |
| `public/apple-touch-icon.png` | Rasterized from supplied `logo-light.svg`, contained on the shared dark canvas. |

The separate Prototype marker in the desktop Develop artwork is exported from `5303:4303` and positioned over the matching diagram. Mobile exports already include their own marker.

Images carry intrinsic dimensions. Portrait/certificates have contextual alternative text; decorative logos, icons, diagrams and hero artwork have empty alt text or an `aria-hidden` parent. Below-fold image loading is lazy. Fonts and all rendered images are local; no expiring Figma asset URLs are required at runtime.

`src/components/vendor/GradientWaves.jsx` and its CSS are the React Bits source downloaded on 6 September 2026. Its license is retained beside it. The supplied Studio parameters are set in `GradientBackground.jsx`; React mounting responds to reduced-motion changes, while the renderer manages offscreen/tab visibility, resizing and cleanup. `RendererBoundary` retains the static background if initialization fails.

The repository's existing license remains unchanged. Third-party code, fonts and brand marks retain their respective rights and notices.
